package sample;

/**
 * This class is added to allow for exporting to a JAR file. When in an editor, it is safe to run Main.java instead.
 * @author pmbos
 */
public class Launcher {
    public static void main(String[] args) {
        Main.main(args);
    }
}
